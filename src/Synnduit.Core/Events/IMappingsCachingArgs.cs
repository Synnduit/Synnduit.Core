namespace Synnduit.Events
{
    /// <summary>
    /// The event data for the event that occurs when source/destination system identifier
    /// mappings are about to be loaded from the database into the in-memory cache.
    /// </summary>
    public interface IMappingsCachingArgs
    {
    }
}
